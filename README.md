# Life Savor Homebrew Tap

Homebrew formulae for Life Savor developer tools.

## Install

```bash
brew tap lifesavorai/tools
brew install lsai-cli
```

## Upgrade

```bash
brew update
brew upgrade lsai-cli
```

## Available Formulae

| Formula | Description |
|---------|-------------|
| `lsai-cli` | Life | `lsai-cli` | Life | `lsai-cli` | Life | `lsai-cli` | Life | `lsai-cli` | Life | `lsI k| `lsai-cli` | Life | `ls whoami       # Verify credentials
lsai-cli status       # Check service status
```
